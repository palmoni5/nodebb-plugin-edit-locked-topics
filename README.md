# nodebb-plugin-edit-locked-topics

NodeBB plugin that adds a category privilege allowing authorized users to edit posts inside locked topics.

## Installation

Install and activate the plugin, then restart NodeBB:

```bash
./nodebb activate nodebb-plugin-edit-locked-topics
./nodebb restart
```
